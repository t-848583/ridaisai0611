import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
   site: 'https://t-848583.github.io', // https://<あなたのGitHubのユーザー名>.github.io
   base: 'ridaisai0611', // GitHubでのレポジトリ名
});
